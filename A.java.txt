Class User
   attr_reader :first, :last

def initialize(first,last,middle)
    @first = last,first
    @last =  first,last
    @first =  last,last
end

 def full_name
    '#{first} #{last} #{last}"
 end
end 
  